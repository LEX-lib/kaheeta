---
phase: 02-encryption-layer
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/wallecx/notesCrypto.ts
  - src/lib/wallecx/notesCrypto.test.ts
autonomous: true
requirements: [ENC-01, ENC-02, ENC-03]
must_haves:
  truths:
    - "encryptBody(key, plaintext) followed by decryptBody(key, output) returns the original plaintext (D-02: AES-GCM-256 via window.crypto.subtle, no new packages)"
    - "Each encryptBody call produces a different ciphertext for the same input (fresh random IV) (D-07: per-call 12-byte IV)"
    - "decryptBody with a wrong key throws OperationError (no silent plaintext leak)"
    - "decryptBody on a legacy Phase 1 plaintext JSON string throws (so callers can trigger fallback), never returns garbage (D-10: lazy migration fallback contract)"
    - "A body larger than 65KB round-trips without a RangeError (loop-based Base64) (D-08: no spread+btoa)"
    - "deriveKey(userId, salt) with the same inputs produces a key that decrypts the same ciphertext"
  artifacts:
    - path: "src/lib/wallecx/notesCrypto.ts"
      provides: "Pure crypto primitives: deriveKey, encryptBody, decryptBody, uint8ToBase64, base64ToUint8"
      exports: ["deriveKey", "encryptBody", "decryptBody"]
      min_lines: 60
    - path: "src/lib/wallecx/notesCrypto.test.ts"
      provides: "Unit tests for round-trip, wrong-key, legacy-plaintext, large-body, key-stability"
      contains: "describe"
  key_links:
    - from: "src/lib/wallecx/notesCrypto.ts encryptBody"
      to: "window.crypto.subtle.encrypt"
      via: "AES-GCM with fresh 12-byte IV"
      pattern: "AES-GCM"
    - from: "src/lib/wallecx/notesCrypto.ts deriveKey"
      to: "window.crypto.subtle.deriveKey"
      via: "PBKDF2 SHA-256 over userId + salt"
      pattern: "PBKDF2"
---

<objective>
Build the pure crypto primitives for note encryption in `src/lib/wallecx/notesCrypto.ts`, test-first. No Vue, no PocketBase — a self-contained module of pure functions that Plans 02 and 03 consume. Delivers the cryptographic core of ENC-01 (AES-GCM 256-bit encryption), ENC-02 (PBKDF2 key derivation), and ENC-03 (in-browser decryption).

Purpose: Isolating the primitives means they can be unit-tested against `window.crypto.subtle` in jsdom without any PocketBase mock. This is the contract layer — Plan 02 (key lifecycle) and Plan 03 (component hooks) build against these exported signatures.

Output: `notesCrypto.ts` (deriveKey, encryptBody, decryptBody + Base64 helpers) and its co-located `notesCrypto.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-encryption-layer/02-CONTEXT.md
@.planning/phases/02-encryption-layer/02-RESEARCH.md
@.planning/phases/02-encryption-layer/02-VALIDATION.md
@./CLAUDE.md

<interfaces>
<!-- Existing test conventions the executor must mirror (Pattern A: co-located .test.ts). -->
<!-- From .planning/codebase/TESTING.md and src/lib/pocketbase/__tests__/notesMapper.spec.ts: -->
<!-- - Explicit vitest imports: `import { describe, it, expect } from 'vitest'` (no globals). -->
<!-- - Factory functions for fixtures with an overrides param. -->
<!-- - No mocking (vi.mock/vi.fn/vi.spyOn are absent from the whole suite). -->
<!-- - `window.crypto.subtle` is confirmed available in the jsdom test env (Node 22.14.0) — no polyfill/setup file needed. -->
<!-- - Run: `npx vitest run src/lib/wallecx/notesCrypto.test.ts` -->

<!-- The Note.body shape being encrypted, from src/types/wallecx/notes/types.d.ts: -->
<!-- body: string  // JSON.stringify of ProseMirror JSONContent, e.g. {"type":"doc","content":[...]} -->

<!-- Reference implementation patterns live in 02-RESEARCH.md Pattern 1 (deriveKey/encryptBody/decryptBody) -->
<!-- and Pitfall 2 (uint8ToBase64 loop). Constants per D-02/D-07: IV=12 bytes, salt=16 bytes, AES-GCM-256. -->
<!-- PBKDF2 iterations = 200_000 (RESEARCH recommendation A2 — document the constant in a comment). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write failing crypto primitive tests</name>
  <files>src/lib/wallecx/notesCrypto.test.ts</files>
  <read_first>
    - src/lib/pocketbase/__tests__/notesMapper.spec.ts (test style: explicit vitest imports, factory functions, describe-per-contract)
    - .planning/phases/02-encryption-layer/02-RESEARCH.md (Pattern 1 signatures; Validation Architecture "Phase Requirements → Test Map")
    - .planning/phases/02-encryption-layer/02-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
    - .planning/codebase/TESTING.md (Pattern A co-located .test.ts, no globals, no mocking)
  </read_first>
  <behavior>
    - Round-trip (ENC-01/ENC-03): a helper derives a key from a fixed userId + fixed salt, then `decryptBody(key, await encryptBody(key, plaintext))` deep-equals the original plaintext string. Test with a realistic JSON body string like `{"type":"doc","content":[{"type":"paragraph"}]}`.
    - Fresh IV (D-07): two `encryptBody(key, sameInput)` calls produce two DIFFERENT ciphertext strings, and BOTH decrypt back to the original.
    - Wrong key (ENC-01, security): decrypting a ciphertext with a key derived from a DIFFERENT userId rejects (expect the promise to reject / throw). Use `await expect(...).rejects.toThrow()`.
    - Legacy-plaintext fallback (D-10): `decryptBody(key, '{"type":"doc"}')` (a raw Phase 1 plaintext body, not valid Base64 ciphertext) rejects — proving callers must catch to trigger the plaintext fallback. Do NOT assert it returns the string; assert it rejects.
    - Large body (D-08): build a plaintext string > 65KB (e.g. `"a".repeat(70000)` wrapped in a JSON string) and assert round-trip succeeds with no RangeError.
    - Key stability (ENC-02): deriving a key twice from the same userId + same salt yields two keys that can each decrypt the other's ciphertext (same effective key).
  </behavior>
  <action>
    Create `src/lib/wallecx/notesCrypto.test.ts` importing `describe, it, expect` from `vitest` and the three primitives from `@/lib/wallecx/notesCrypto`. Import `deriveKey`, `encryptBody`, `decryptBody`. Write a local `async function makeKey(userId = 'user000000abc12', saltSeed = 1)` factory that builds a 16-byte `Uint8Array` salt deterministically (fill with saltSeed) and calls `deriveKey`. Cover every case in the behavior block with explicit describe blocks. Use `globalThis.crypto` where you need salt bytes in a test. This is the RED commit — the source module does not exist yet, so the import fails and all tests error. Commit with message `test(02-01): add failing notesCrypto primitive tests`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wallecx/notesCrypto.test.ts 2>&1 | grep -qE "Cannot find module|Failed to resolve|notesCrypto" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/wallecx/notesCrypto.test.ts` exists and imports `deriveKey`, `encryptBody`, `decryptBody` from `@/lib/wallecx/notesCrypto`.
    - Running the test file fails because the source module is missing (RED state confirmed) — the run is red, not green.
    - Test file uses explicit vitest imports (no globals) and contains a factory function for building keys.
    - A commit `test(02-01): add failing notesCrypto primitive tests` exists.
  </acceptance_criteria>
  <done>Test file exists covering all six behaviors; test run is RED because notesCrypto.ts does not yet exist; RED commit made.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement notesCrypto primitives</name>
  <files>src/lib/wallecx/notesCrypto.ts</files>
  <read_first>
    - src/lib/wallecx/notesCrypto.test.ts (the tests from Task 1 — the contract to satisfy)
    - .planning/phases/02-encryption-layer/02-RESEARCH.md (Pattern 1 full implementation + Pitfall 2 uint8ToBase64 loop + Pitfall 7 noUncheckedIndexedAccess)
    - .planning/phases/02-encryption-layer/02-CONTEXT.md (D-02, D-07, D-08 — IV/salt sizes, loop-based Base64)
    - ./CLAUDE.md (import type rule, @/ alias, noUncheckedIndexedAccess)
  </read_first>
  <action>
    Create `src/lib/wallecx/notesCrypto.ts` as a pure module (no Vue, no PocketBase imports). Define module constants: `IV_LENGTH = 12`, `SALT_LENGTH = 16`, `PBKDF2_ITERATIONS = 200_000` (add a comment citing OWASP guidance and the UX-balance rationale from RESEARCH A2). Export `async function deriveKey(userId: string, salt: Uint8Array): Promise<CryptoKey>` — importKey the UTF-8 bytes of userId as `PBKDF2` raw key material (extractable false, usage `['deriveKey']`), then `subtle.deriveKey` with PBKDF2/SHA-256/salt/iterations into an `AES-GCM` 256-bit key, non-extractable, usages `['encrypt','decrypt']`. Export `async function encryptBody(key: CryptoKey, plaintext: string): Promise<string>` — generate a fresh 12-byte IV via `crypto.getRandomValues`, encrypt the UTF-8 bytes, allocate a `Uint8Array(IV_LENGTH + ciphertext.byteLength)`, set IV at offset 0 and ciphertext at offset 12, then encode via the loop-based `uint8ToBase64` helper (NOT spread+btoa — D-08). Export `async function decryptBody(key: CryptoKey, b64: string): Promise<string>` — decode Base64 to bytes via `base64ToUint8`, slice IV from first 12 bytes and ciphertext from the rest, `subtle.decrypt` (let OperationError propagate — do NOT catch here; callers own the fallback per D-10), return `TextDecoder().decode(...)`. Add private helpers `uint8ToBase64(bytes: Uint8Array): string` (loop with `bytes[i] ?? 0` for noUncheckedIndexedAccess) and `base64ToUint8(b64: string): Uint8Array` (`Uint8Array.from(atob(b64), c => c.charCodeAt(0))`). Reference `window.crypto.subtle` via a `const subtle = crypto.subtle` at module top so it also resolves under jsdom's `globalThis.crypto`. This is GREEN — run the tests until all pass. Commit `feat(02-01): implement notesCrypto AES-GCM + PBKDF2 primitives`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wallecx/notesCrypto.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/wallecx/notesCrypto.ts` exists and exports `deriveKey`, `encryptBody`, `decryptBody`.
    - `npx vitest run src/lib/wallecx/notesCrypto.test.ts` passes all tests (GREEN).
    - Source contains `AES-GCM` and `PBKDF2` and a loop-based Base64 encoder (no `String.fromCharCode(...` spread over the full byte array).
    - `decryptBody` does NOT wrap `subtle.decrypt` in a try/catch (OperationError must propagate to callers per D-10).
    - No import of `pb`, `pocketbase`, or any Vue module in this file.
    - A commit `feat(02-01): implement notesCrypto AES-GCM + PBKDF2 primitives` exists.
  </acceptance_criteria>
  <done>All notesCrypto tests pass; primitives implemented per D-02/D-07/D-08; OperationError propagates; GREEN commit made.</done>
</task>

<task type="auto">
  <name>Task 3: Type-check the module</name>
  <files>src/lib/wallecx/notesCrypto.ts</files>
  <read_first>
    - src/lib/wallecx/notesCrypto.ts (the module just written)
    - ./CLAUDE.md (vue-tsc requirement, noUncheckedIndexedAccess)
  </read_first>
  <action>
    Run the project type checker over the whole workspace to confirm the new module compiles under `noUncheckedIndexedAccess`. If `vue-tsc` reports errors in notesCrypto.ts (most likely `bytes[i]` being `number | undefined`, or `CryptoKey`/`Uint8Array` typing), fix them in place — do not suppress with `// @ts-ignore`. Re-run until clean. No new commit needed if Task 2 already type-checks; if a fix was required, commit `fix(02-01): satisfy noUncheckedIndexedAccess in notesCrypto`.
  </action>
  <verify>
    <automated>npx vue-tsc --build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vue-tsc --build` exits 0 with no errors referencing `src/lib/wallecx/notesCrypto.ts`.
    - No `@ts-ignore` or `@ts-expect-error` added to notesCrypto.ts.
  </acceptance_criteria>
  <done>Type check passes cleanly for the new module under the project's strict config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PocketBase | Note body/snippet must be opaque ciphertext before crossing this boundary (this plan produces the bytes; Plan 03 sends them) |
| in-memory plaintext → ciphertext | encryptBody is the only sanctioned path from plaintext to stored bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | encryptBody wire format | mitigate | AES-GCM-256 with per-call fresh 12-byte IV (D-07); server sees only Base64 ciphertext. Test asserts ciphertext differs per call and is not the plaintext JSON. |
| T-02-02 | Tampering | decryptBody | mitigate | AES-GCM 128-bit auth tag; tampered/wrong-key ciphertext throws OperationError. Test asserts wrong-key rejects. |
| T-02-03 | Cryptographic weakness | deriveKey | mitigate | PBKDF2-SHA256 at 200_000 iterations over userId+salt (never token — D-01). Salt is 16 random bytes (D-06). |
| T-02-04 | Denial of Service | uint8ToBase64 on large bodies | mitigate | Loop-based Base64 (D-08) avoids RangeError on >65KB bodies; test covers a 70KB body round-trip. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan (D-02: window.crypto.subtle is built-in). No install task exists. |
</threat_model>

<verification>
- `npx vitest run src/lib/wallecx/notesCrypto.test.ts` — all crypto primitive tests green.
- `npx vue-tsc --build` — exit 0, no type errors.
- Manual grep: `AES-GCM`, `PBKDF2`, and a loop-based Base64 encoder present in notesCrypto.ts.
</verification>

<success_criteria>
- `notesCrypto.ts` exports `deriveKey`, `encryptBody`, `decryptBody` as pure functions with zero Vue/PocketBase coupling.
- Round-trip, fresh-IV, wrong-key-rejects, legacy-plaintext-rejects, >65KB, and key-stability behaviors are all proven by passing tests.
- OperationError propagates out of `decryptBody` (callers own the D-10 fallback).
- Module compiles under `noUncheckedIndexedAccess`.
</success_criteria>

<output>
Create `.planning/phases/02-encryption-layer/02-01-SUMMARY.md` when done.
</output>
