---
phase: 02-encryption-layer
plan: "03"
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/wallecx/ManageNote.vue
  - src/components/wallecx/NotesTab.vue
autonomous: false
requirements: [ENC-01, ENC-03]
must_haves:
  truths:
    - "Creating or editing a note stores AES-GCM ciphertext in the body AND snippet fields of kaheeta_notes (never plaintext) (D-09: snippet encrypted too)"
    - "Opening an encrypted note decrypts the body in-browser and renders the original rich text with no user action"
    - "A Phase 1 plaintext note opens without crashing (lazy fallback) and re-saves as ciphertext on next edit (D-10: OperationError → plaintext fallback → toast.error, resolves WR-01)"
    - "The notes list shows a readable snippet preview for both encrypted and legacy plaintext notes"
    - "The user is never prompted for a separate encryption password anywhere in the flow (D-04: no second password — accepted tradeoff)"
  artifacts:
    - path: "src/components/wallecx/ManageNote.vue"
      provides: "Encrypt-on-write saveFn (body+snippet) and async decrypt-on-load with D-10 fallback"
      contains: "encryptBody"
    - path: "src/components/wallecx/NotesTab.vue"
      provides: "Per-note snippet decryption after list fetch with legacy plaintext fallback"
      contains: "decryptBody"
  key_links:
    - from: "src/components/wallecx/ManageNote.vue saveFn"
      to: "src/lib/wallecx/notesCrypto.ts encryptBody"
      via: "encrypt body + snippet before mapToUpdateNote"
      pattern: "encryptBody"
    - from: "src/components/wallecx/ManageNote.vue onMounted"
      to: "src/lib/wallecx/notesCrypto.ts decryptBody"
      via: "decrypt props.note.body, catch OperationError → plaintext JSON fallback"
      pattern: "decryptBody"
    - from: "src/components/wallecx/NotesTab.vue"
      to: "src/composables/useNotesCrypto.ts getOrDeriveKey"
      via: "derive key once, decrypt each snippet"
      pattern: "getOrDeriveKey"
---

<objective>
Wire the crypto layer into the UI: `ManageNote.vue` encrypts body + snippet before every PocketBase write and decrypts the body on load; `NotesTab.vue` decrypts each note's snippet for the list preview. Both apply the D-10 lazy-migration fallback so Phase 1 plaintext notes render without crashing (also resolving Phase 1 code-review finding WR-01, the unguarded `JSON.parse`). Delivers ENC-01 (ciphertext on write) and ENC-03 (in-browser decrypt on read) end-to-end.

Purpose: This is the integration seam. Plans 01 and 02 are inert until these two components call `encryptBody`/`decryptBody` through the session key. After this plan, the server never receives plaintext note content.

Output: modified `ManageNote.vue` (async load + encrypted save) and `NotesTab.vue` (snippet decrypt), plus a human-verify checkpoint confirming ciphertext-at-rest in the Admin UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-encryption-layer/02-CONTEXT.md
@.planning/phases/02-encryption-layer/02-RESEARCH.md
@./CLAUDE.md

@src/components/wallecx/ManageNote.vue
@src/components/wallecx/NotesTab.vue

<interfaces>
<!-- Plan 01 (src/lib/wallecx/notesCrypto.ts): -->
<!--   encryptBody(key: CryptoKey, plaintext: string): Promise<string>   // Base64 IV+ciphertext -->
<!--   decryptBody(key: CryptoKey, b64: string): Promise<string>         // throws OperationError on wrong/legacy input -->
<!-- Plan 02 (src/composables/useNotesCrypto.ts): -->
<!--   useNotesCrypto() → { getOrDeriveKey(): Promise<CryptoKey>, clearKey(): void }   // cached per session -->

<!-- Existing ManageNote.vue (Phase 1) key spots being changed: -->
<!--   L56-58: editorContent init = props.note.body ? JSON.parse(props.note.body) : null  ← unguarded parse (WR-01), becomes async decrypt -->
<!--   L61-89: saveFn builds snippet via generateText(...).slice(0,150), body via JSON.stringify(editorContent), -->
<!--           then mapToUpdateNote({...record.value, body, snippet}) then pb.create/update. Encrypt body+snippet before mapToUpdateNote. -->
<!--   useAutoSave(saveFn, 1000) already serializes saves (CR-02 in-flight guard) — saveFn may become async-await heavier; that is fine. -->

<!-- Existing NotesTab.vue (Phase 1) key spots: -->
<!--   L22-31: notes.value = await instrumentedGetFullList<Note>('kaheeta_notes', {...})  ← decrypt snippets after this -->
<!--   L107-109: template renders note.snippet in the card preview. -->
<!--   useToast() available as `toast`; toast.error for failures (D-10). -->
<!--   NotesTab uses top-level await (Suspense) — the snippet decrypt can be awaited in that same top-level flow. -->

<!-- D-10 fallback contract (LOCKED): decryptBody throws OperationError for legacy plaintext. -->
<!--   Body path: catch → treat props.note.body as raw plaintext JSON → JSON.parse; if THAT throws → toast.error, render empty, no crash. -->
<!--   Snippet path: catch → use the raw stored snippet string as-is (legacy plaintext preview). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encrypt-on-write + decrypt-on-load in ManageNote.vue</name>
  <files>src/components/wallecx/ManageNote.vue</files>
  <read_first>
    - src/components/wallecx/ManageNote.vue (current saveFn L61-89 and editorContent init L56-58)
    - src/lib/wallecx/notesCrypto.ts (encryptBody, decryptBody signatures)
    - src/composables/useNotesCrypto.ts (getOrDeriveKey)
    - src/composables/useToast.ts (toast.error signature — see how NotesTab imports it)
    - .planning/phases/02-encryption-layer/02-CONTEXT.md (D-09 encrypt snippet, D-10 lazy fallback)
    - .planning/phases/02-encryption-layer/02-RESEARCH.md (Pattern 3 write, Pattern 4 read)
  </read_first>
  <action>
    In `<script setup>`: import `encryptBody, decryptBody` from `@/lib/wallecx/notesCrypto`, `useNotesCrypto` from `@/composables/useNotesCrypto`, and `useToast` from `@/composables/useToast`. Instantiate `const { getOrDeriveKey } = useNotesCrypto()` and `const toast = useToast()`.

    DECRYPT-ON-LOAD: Replace the synchronous `editorContent` initializer (currently `props.note ? (props.note.body ? JSON.parse(...) : null) : null` — the unguarded parse is WR-01) with `const editorContent = ref<JSONContent | null>(null)`. Add `const isDecrypting = ref(true)`. In an `onMounted(async () => {...})`: if `!props.note?.body`, set `isDecrypting=false` and return. Otherwise `const key = await getOrDeriveKey()`; inner try: `plainBody = await decryptBody(key, props.note.body)`; inner catch: fallback `plainBody = props.note.body` (legacy plaintext). Then `editorContent.value = plainBody ? JSON.parse(plainBody) as JSONContent : null` — wrap this JSON.parse so that if the fallback body is ALSO not valid JSON, catch it, call `toast.error('Could not open this note — it may be corrupted.')`, and leave editorContent null (do NOT crash — D-10). Wrap the whole getOrDeriveKey/decrypt flow in an outer try/catch that toasts on unexpected errors, and `finally { isDecrypting.value = false }`. Import `onMounted` from vue.

    ENCRYPT-ON-WRITE: In `saveFn`, after computing `rawContent`, `plainBody = JSON.stringify(editorContent.value)` and the existing `snippet` (generateText slice 150): get `const key = await getOrDeriveKey()` (cached, instant after first call), then `const body = await encryptBody(key, plainBody)` and `const encSnippet = await encryptBody(key, snippet)` (D-09 — snippet uses the same scheme). Pass `body` and `encSnippet` into `mapToUpdateNote({ ...record.value, body, snippet: encSnippet })`. Leave the create/update branching, `Object.assign`, and `emit('note-saved', ...)` unchanged. Note: `record.value` now holds ciphertext body/snippet after save — that is correct; it matches what the server stores.

    OPTIONAL UX: guard the NoteEditor `v-if` with `isDecrypting` so the editor mounts only after decryption resolves — e.g. `v-if="visible && !isDecrypting"`. Keep the existing `visible` guard semantics.
  </action>
  <verify>
    <automated>npx vue-tsc --build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - ManageNote.vue imports `encryptBody`, `decryptBody`, `useNotesCrypto`, and `useToast`.
    - The synchronous unguarded `JSON.parse(props.note.body)` initializer is gone; decryption happens in an `onMounted` async handler with a try/catch that calls `toast.error` and never rethrows (WR-01 + D-10 resolved).
    - `saveFn` calls `encryptBody` for BOTH body and snippet (D-09) before `mapToUpdateNote`, and no plaintext `JSON.stringify(editorContent.value)` value is passed to `mapToUpdateNote`.
    - No reference to `pb.authStore.token`.
    - `npx vue-tsc --build` exits 0 and `npx vitest run` passes (existing suite still green — notesCrypto tests + notesMapper spec).
  </acceptance_criteria>
  <done>ManageNote encrypts body+snippet on save and decrypts on load with a non-crashing legacy fallback; type-checks and the suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Decrypt snippets in NotesTab.vue list</name>
  <files>src/components/wallecx/NotesTab.vue</files>
  <read_first>
    - src/components/wallecx/NotesTab.vue (current top-level await fetch L22-31, template snippet L107-109)
    - src/lib/wallecx/notesCrypto.ts (decryptBody)
    - src/composables/useNotesCrypto.ts (getOrDeriveKey)
    - .planning/phases/02-encryption-layer/02-CONTEXT.md (D-09, D-10)
    - .planning/phases/02-encryption-layer/02-RESEARCH.md (Pattern 5 snippet decrypt + Pitfall 6 await key before loop)
  </read_first>
  <action>
    Import `decryptBody` from `@/lib/wallecx/notesCrypto` and `useNotesCrypto` from `@/composables/useNotesCrypto`; add `const { getOrDeriveKey } = useNotesCrypto()`. Add an `async function decryptSnippets(rawNotes: Note[]): Promise<Note[]>`: `const key = await getOrDeriveKey()` FIRST (Pitfall 6 — derive before the map, do not call inside the loop), then `return Promise.all(rawNotes.map(async (note) => { if (!note.snippet) return note; try { return { ...note, snippet: await decryptBody(key, note.snippet) } } catch { return { ...note, snippet: note.snippet } } }))` — the catch keeps the legacy plaintext snippet as-is (D-10). In the existing top-level try block, after `instrumentedGetFullList` resolves into a local `rawNotes`, set `notes.value = await decryptSnippets(rawNotes)`. Keep the existing `toast.error('Failed to load notes.')` catch. Do not change the sorting computed or the template.
    Note: `handleNoteSaved` receives a note whose snippet is now ciphertext (from ManageNote after save). Decrypt that snippet before splicing/unshifting into `notes.value` so the list preview stays readable without a reload — decrypt it via the same key/try-catch pattern (make `handleNoteSaved` async or decrypt inline).
  </action>
  <verify>
    <automated>npx vue-tsc --build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - NotesTab.vue imports `decryptBody` and `useNotesCrypto`, and calls `getOrDeriveKey()` once before the snippet map (not inside it).
    - The list-load path decrypts snippets with a per-note try/catch that falls back to the raw snippet on failure (legacy plaintext, D-10).
    - `handleNoteSaved` decrypts the incoming ciphertext snippet before updating `notes.value` so the freshly saved note shows a readable preview.
    - `npx vue-tsc --build` exits 0 and `npx vitest run` passes.
  </acceptance_criteria>
  <done>List snippets decrypt for encrypted notes and fall back gracefully for legacy notes; freshly saved notes show readable previews; type-checks and suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>ManageNote now encrypts body + snippet before every write and decrypts on load; NotesTab decrypts snippets for the list. This checkpoint confirms the end-to-end result against a live PocketBase backend (the manual-only verifications from 02-VALIDATION.md that unit tests cannot cover).</what-built>
  <how-to-verify>
    1. Run `npm run dev` and log in, then go to `/wallet` → Notes.
    2. Create a new note with a title and rich-text body (bold, a heading, a bullet). Wait for the "Saved" indicator. Confirm NO password/passphrase prompt appeared at any point (ENC-02).
    3. In the PocketBase Admin UI, open the just-created `kaheeta_notes` record. Confirm BOTH `body` and `snippet` are opaque Base64 ciphertext, not readable JSON/text (ENC-01, D-09). Confirm a `kaheeta_user_settings` record now exists for your user with a Base64 `note_encryption_salt`.
    4. Reopen the note in the app. Confirm the rich-text body renders exactly as typed, with no user action (ENC-03). Confirm the notes-list preview shows readable snippet text, not Base64.
    5. Legacy check (if a Phase 1 plaintext note exists, or create one by writing plaintext JSON directly into a kaheeta_notes body via Admin UI): open it in the app. Confirm it renders without crashing and, after any edit + auto-save, its body/snippet become ciphertext in the Admin UI (lazy migration, D-10).
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe what failed (e.g. plaintext visible in body, snippet shows Base64 in the list, note failed to open).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ManageNote saveFn → PocketBase | The last point where plaintext could leak; must send ciphertext only |
| PocketBase → ManageNote/NotesTab load | Untrusted stored bytes (could be ciphertext, legacy plaintext, or corrupt) cross into decrypt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | saveFn write payload | mitigate | Both body and snippet encrypted before mapToUpdateNote (D-09). Human-verify confirms ciphertext-at-rest in Admin UI. |
| T-02-10 | Denial of Service | ManageNote load on legacy/corrupt body | mitigate | decryptBody OperationError caught → plaintext JSON fallback → JSON.parse wrapped → toast.error + empty editor, never crashes (D-10, resolves WR-01). |
| T-02-11 | Denial of Service | NotesTab snippet decrypt loop | mitigate | Per-note try/catch falls back to raw snippet; getOrDeriveKey awaited once before loop (Pitfall 6) so a single derivation failure surfaces via the existing load-failed toast, not per-row crashes. |
| T-02-12 | Information Disclosure | plaintext left in component state | accept | editorContent holds decrypted plaintext in memory while the dialog is open (unavoidable for editing); XSS/CSP hardening is out of scope for this phase (RESEARCH threat model boundary). |
| T-02-SC | Tampering | npm installs | mitigate | No packages installed (D-02). No install task exists. |
</threat_model>

<verification>
- `npx vue-tsc --build` — exit 0 (both components).
- `npx vitest run` — full suite green (notesCrypto + notesMapper + all prior tests).
- Grep ManageNote.vue for `encryptBody` and `decryptBody` (present) and absence of the synchronous `JSON.parse(props.note.body)` initializer.
- Grep NotesTab.vue for `decryptBody` and `getOrDeriveKey` (present).
- Human-verify checkpoint: ciphertext-at-rest, transparent decrypt, no password prompt, legacy note opens (02-VALIDATION.md manual-only table).
</verification>

<success_criteria>
- Every note write stores ciphertext in both body and snippet (ENC-01, D-09).
- Encrypted notes decrypt transparently on open and in the list preview (ENC-03).
- Phase 1 plaintext notes open without crashing and upgrade to ciphertext on next edit (D-10, WR-01 resolved).
- No second password prompt anywhere (ENC-02).
</success_criteria>

<output>
Create `.planning/phases/02-encryption-layer/02-03-SUMMARY.md` when done.
</output>
