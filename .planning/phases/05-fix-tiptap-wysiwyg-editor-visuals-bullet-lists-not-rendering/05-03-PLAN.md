---
phase: 05-fix-tiptap-editor-visuals
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-03]
must_haves:
  truths:
    - "Creating a bullet list shows visible markers + indentation; nested lists indent; ordered lists show numbers; renders the same on reopen"
    - "Headings, bold, italic, and links render with consistent, intentional styling in both light and dark mode (no theme bleed / stray margins)"
    - "The text caret is visible at all times while editing, in both light and dark themes, including on empty lines and inside list items"
    - "No regression: existing notes still load, edit, save, and decrypt correctly (Phases 1-4 behavior intact)"
  artifacts: []
  key_links: []
---

<objective>
Human visual UAT of the editor fixes across light and dark themes. This is the acceptance gate
for the pure-CSS success criteria that cannot be unit-tested, plus a no-regression pass over the
Phase 1-4 note lifecycle. Mirrors the Phase 3 / Phase 4 human-UAT checkpoint pattern.

Purpose: Plans 05-01 (OrderedList + snippet round-trip) and 05-02 (list/typography/caret CSS)
deliver the behavior; a human must confirm the rendering is correct in both themes and that
nothing regressed. Covers all four ROADMAP success criteria and CONTEXT decisions D-01/D-02/D-03.

Output: A pass/fail UAT record. If issues are found, they feed a gap-closure planning pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-fix-tiptap-wysiwyg-editor-visuals-bullet-lists-not-rendering/05-CONTEXT.md
@.planning/phases/05-fix-tiptap-wysiwyg-editor-visuals-bullet-lists-not-rendering/05-01-SUMMARY.md
@.planning/phases/05-fix-tiptap-wysiwyg-editor-visuals-bullet-lists-not-rendering/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start the dev server and run the automated gate before human review</name>
  <files></files>
  <read_first>
    - .planning/phases/05-fix-tiptap-wysiwyg-editor-visuals-bullet-lists-not-rendering/05-01-SUMMARY.md
    - .planning/phases/05-fix-tiptap-wysiwyg-editor-visuals-bullet-lists-not-rendering/05-02-SUMMARY.md
    - CLAUDE.md (npm run dev serves http://localhost:5173; requires VITE_API_BASE_URL in .env)
  </read_first>
  <action>
    Confirm the automated gate is green before asking the human to look: run `npx vitest run`
    (expect 0 failures — existing 143 tests plus the new noteSnippet tests) and `npm run type-check`
    (expect exit 0). Then start the Vite dev server (`npm run dev`) so the human can exercise the
    editor at http://localhost:5173 (log in, open the wallet Notes tab). Report the URL and confirm
    the server is up before the checkpoint.
  </action>
  <verify>
    <automated>npx vitest run && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0
    - `npm run type-check` exits 0
    - Dev server is running and the Notes editor is reachable at http://localhost:5173/wallet
  </acceptance_criteria>
  <done>Automated gate green and dev server running, ready for human visual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual UAT — lists, typography, caret, no-regression (light + dark)</name>
  <what-built>
    OrderedList (numbered-list) support with a toolbar button, and restored editor CSS: visible
    bullet/numbered/nested list markers + indentation, consistent heading/paragraph spacing, and a
    global amber text caret visible in both light and dark themes.
  </what-built>
  <how-to-verify>
    In the running app (http://localhost:5173), log in and open a note in the Notes tab. Verify in
    BOTH light and dark mode (toggle the theme switch in the navbar):

    1. Bullet list: click the bullet-list toolbar button, type two items — confirm visible disc
       markers and indentation.
    2. Numbered list: click the new numbered-list toolbar button (mdi:format-list-numbered), type
       two items — confirm visible 1. / 2. numbers and indentation.
    3. Nested list: inside a list item, press Tab (or the editor's nesting gesture) to create a
       nested list — confirm it indents a further level.
    4. Caret: click into an empty paragraph, an empty list item, and the end of a heading — confirm
       the text caret (cursor) is clearly visible (amber) in BOTH themes, including on empty lines.
    5. Typography: add an H1, H2, H3, some bold, italic, and a link — confirm consistent, intentional
       spacing with no stray/collapsed margins and no theme bleed (correct colors in each theme).
    6. Persistence: save the note, close and reopen it — confirm bullets/numbers/nesting render the
       same on reopen (round-trips through the encrypted body + snippet).
    7. No regression: open a pre-existing note — confirm it still loads, decrypts, edits, and saves
       correctly (Phases 1-4 behavior intact); confirm the list preview snippet in the Notes list
       still shows plaintext (now including any numbered-list text).
  </how-to-verify>
  <acceptance_criteria>
    - All 7 checks pass in BOTH light and dark mode
    - Any failure is reported with the specific check number + theme so it can be turned into a gap
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 7 checks pass in both themes, or describe the specific failures (check # + light/dark).</resume-signal>
</task>

</tasks>

<threat_model>
No new attack surface — this plan is verification only (no code or CSS changes). STRIDE register:
N/A. The supply-chain consideration for the ordered-list package was dispositioned in plan 05-01
(T-05-SC, accept — first-party @tiptap sibling).
</threat_model>

<verification>
- Automated gate green (vitest + type-check) before human review
- Human confirms all 7 visual/behavioral checks in both light and dark themes
</verification>

<success_criteria>
- All four ROADMAP Phase 5 success criteria confirmed by human UAT in both themes
- No regression to the existing note load/edit/save/decrypt lifecycle
</success_criteria>

<output>
Create `.planning/phases/05-fix-tiptap-wysiwyg-editor-visuals-bullet-lists-not-rendering/05-03-SUMMARY.md` when done
</output>
